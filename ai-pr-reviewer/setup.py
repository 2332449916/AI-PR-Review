"""Setup script for ai-pr-reviewer.

This exists for compatibility with `pip install -e .` development installs.
The primary configuration is in pyproject.toml.
"""

from setuptools import setup

setup()
